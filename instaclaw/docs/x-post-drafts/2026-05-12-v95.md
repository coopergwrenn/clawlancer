# X-post drafts — v95 (2026-05-12)

Generated by `scripts/generate-version-post.ts` (mode: release, model: claude-sonnet-4-6).
Source changelog: `docs/changelog-latest.md`.
Style guide: `docs/x-post-style-guide.md`.

---

## Variant A — tight 5-7 tweet thread (user-facing wins)

1/

@instaclaws v92 → v95 🦀 — 48 commits, 2 days, live fleet the whole time.

⏱️ your agent now acknowledges you instantly — no more 3-minute silences
🧠 Edge City agents' full context finally loads — 1,054 chars were silently missing
🛡️ the reconciler stopped lying about which VMs were healthy
📨 your Telegram bot heals itself if its token drifts out of sync

the release where the fleet stopped gaslighting us.

2/

⏱️ three-layer acknowledgment UX

prompt → agent silence was 3 minutes on a real session may 11. edge Esmeralda ships may 30 to 1,000 attendees — that experience would have churned the room. v95 adds a three-layer Telegram ack so the agent responds immediately, even while it's still thinking. ~2,000-line PRD behind it.

3/

🧠 Edge City context truncation — fixed

all 5 production edge_city VMs were silently truncating SOUL.md at 35,000 chars. the onboarding interview tail and the entire Consensus section never reached the agent's bootstrap context — ~1,054 chars dropped every restart. v92 patched the ceiling to 40,000. v93 added a budget-aware over-budget check so it can't drift again.

4/

🩹 lying-DB rate — from 27% to zero

12 of 44 healthy cv≥88 VMs were reporting stale state to the DB. reconciler thought they were behind; they weren't. root-cause fixes landed, 10 VMs got a cv-reset, fleet census reprobe confirmed clean. the fleet now auto-quarantines persistent failures instead of silently carrying them.

5/

📨 Telegram self-healing

some VMs had a disk↔DB `telegram_bot_token` mismatch — agent was reachable by the platform but unreachable by the user. v95 (Rule 34) self-heals the drift on reconcile. also: one-shot backfill for VMs missing `channels.telegram.botToken` on disk entirely. your bot just works now.

6/

🌐 Edge City gets a real home

/edge-city → /edge — Edge City visual language, login-state-aware routing, branded /signup + /connect for Edge Esmeralda attendees, open graph share card. ships ahead of may 30.

7/

Next: matching engine outcomes ingest (§5.2 infrastructure merged).

agents that don't just introduce — they track whether the meeting happened. feedback loop into the ranking model. PRD in progress.

48 commits. 2 days. Onward.

-----

## Variant B — 12-18 tweet detailed thread (technical audience)

1/

@instaclaws v92 → v95 🦀 — 48 commits across 2026-05-10 → HEAD.

⏱️ three-layer Telegram ack — no more silent minutes
🧠 SOUL.md truncation fixed fleet-wide — 1,054 chars restored to 5 edge_city VMs
🩹 27% lying-DB rate — root-caused and reset
📨 Telegram token drift — self-heals on reconcile (Rule 34)
⚙️ gbrain rolled out fleet-wide via new reconciler step
🌐 /edge-city → /edge — Edge Esmeralda ready
📜 2 new rules in CLAUDE.md, each tracing to a real incident

the release where the fleet stopped lying to itself.

2/

2 days. 48 commits. 3 manifest versions (v92 → v93 → v95). 16 reconciler changes. 22 infrastructure changes. 4 user-facing features. 2 emergency manifest patches. 1 new reconciler step (stepGbrain). 2 new CLAUDE.md rules. ~2,000-line PRD for the ack UX alone.

all shipped to a live fleet with paying customers using their agents throughout.

3/

⏱️ agent acknowledgment UX

originating bug: 3 minutes of silence between a real prompt and the agent's response — 9:07 PM 2026-05-11. edge Esmeralda (1,000 attendees, ships 2026-05-30) would have churned that at scale. v95 ships a three-layer ack: immediate receipt, mid-work pulse, final delivery. PRD: docs/prd/agent-acknowledgment-ux-2026-05-11.md.

4/

🧠 SOUL.md truncation — silent and fleet-wide

all 5 production edge_city VMs had 36,054 chars of SOUL.md content against a 35,000-char `bootstrapMaxChars` ceiling. the last ~1,054 chars — the Edge onboarding interview tail + the entire Consensus section — were silently dropped from bootstrap context on every restart. no error. no log line. just a shorter agent.
<!-- exceeds 280 — split or Premium -->

5/

🧠 (cont.) how we fixed it

v92: partner-stub migration step + emergency `bootstrapMaxChars` 35000 → 40000. simultaneously disabled `CONSENSUS_MATCHING_AWARENESS_V1` SOUL.md append as a second emergency patch while the migration ran. v93 added the APPEND branch — if a partner section is missing (provisioned before the template existed, or a configure failure left it out), we add it. budget-aware over-budget check prevents future drift.
<!-- exceeds 280 — split or Premium -->

6/

🩹 lying-DB rate — 27%, root-caused

12 of 44 healthy cv≥88 VMs were reporting stale config versions to the DB. reconciler treated them as behind manifest and kept trying to advance them — wasted cycles, noisy alerts, false quarantine risk. root-cause fixes landed in the reconciler. 10 VMs got a cv-reset via Phase 4 script. fleet census reprobe confirmed clean.
<!-- exceeds 280 — split or Premium -->

7/

⚙️ reconciler hardening — 4 changes that matter

`stepSystemdUnit` now uses md5 hash compare instead of filename match — this likely unsticks the cv=82 cohort that's been wedged for weeks. catch-up script added for VMs stuck >N versions behind manifest (Vercel cron can't catch up multi-version drift on its own). hot-reload classification added. auto-restart guardrail added. persistent failure tracking → auto-quarantine → alert.
<!-- exceeds 280 — split or Premium -->

8/

📨 Telegram token drift — self-healing

two related bugs. first: disk `channels.telegram.botToken` and DB `telegram_bot_token` could drift out of sync — agent unreachable by users even though the platform thought it was healthy. Rule 34 in CLAUDE.md. reconciler now self-heals the mismatch on every pass. second: some VMs were missing the disk token entirely — one-shot backfill script deployed.

9/

⚙️ gbrain — fleet-wide rollout

gbrain install pipeline + forensic handoff + 3 ops crons shipped in an earlier commit. Phase 4c added `stepGbrain` as a first-class reconciler step with build-time script embedding — gbrain is now part of the standard manifest, not a side-channel install. coverage-check cron + edge_city readiness probe confirm coverage before Esmeralda.
<!-- exceeds 280 — split or Premium -->

10/

🛡️ model routing — stopped silently downgrading

the gateway proxy was silently routing real user messages to MiniMax on a slice of the fleet. users had no visibility — from the outside, the agent just felt different. fixed in the gateway proxy layer. the agent your customer is talking to is the one they configured.

11/

🌐 /edge-city → /edge

11 files. Edge City visual language throughout. login-state-aware routing — logged-in users land differently than new attendees. branded /signup + /connect pages for Edge Esmeralda. open graph share card for /edge. plaza page moved to /edge/ to match post-rebrand routing. all live ahead of may 30.

12/

🩹 onboarding loop — broken and recovered

post-checkout configure could enter a partial-failure loop — VM stuck in a limbo state, user sees nothing. Rule 33 in CLAUDE.md. fix breaks the loop and recovers from partial configure failures cleanly. also: auth fix — partner cookie was not applying to existing users, only new signups. plugged.

13/

📈 matching engine — §5.2 infrastructure merged

49-file merge. outcomes ingest for the matching pool — the infrastructure to record whether intros actually converted to meetings. feeds back into ranking. this is the foundation the v2 matching engine sits on.

14/

📜 discipline codified

Rule 33: break post-checkout loops + recover from configure partial-failure. Rule 34: self-heal disk↔DB telegram token mismatch. both trace to real production incidents within this 2-day window. CLAUDE.md grows by incident, not by convention.

15/

Next: v2 matching engine — outcomes feedback loop closes.

agents track whether the intro became a meeting. that signal re-ranks future candidates. the model improves from every Esmeralda conversation.

Edge Esmeralda ships 2026-05-30. 1,000 attendees. the ack UX and the gbrain coverage check have to be clean before then.

48 commits. 2 days. Onward.

-----

## Variant C — single banger tweet

@instaclaws v92 → v95 — 48 commits in 2 days.

the headline: 3 minutes of agent silence → instant three-layer Telegram ack. 5 edge_city VMs had 1,054 chars silently truncated from their bootstrap context — fixed. 27% of healthy VMs were lying to the DB about their state — root-caused and reset. Edge Esmeralda (1,000 attendees, may 30) needed all three to be clean. they are.

-----

## Notes

**What was intentionally excluded and why:**

- **`session-backups` bloat probe + emergency purge** — internal ops hygiene, no user-facing outcome to frame it around. worth a separate ops post if the bloat was severe.
- **`--no-strict` opt-in flag for fleet rollout** — too narrow; only relevant to the internal soul-v2 migration tooling.
- **`process.exit()` bypasses `finally` / cron lock leaks** — important engineering, but too deep in internal migration tooling for a public thread. could anchor an open-source "lessons from fleet ops" post.
- **Whitelist race on concurrent `migrateOne` calls** — same reason; internal migration concern.
- **`idempotency gate on session-backup creation`** — no user-facing hook that's clean to explain briefly.
- **Snapshot-bake toolchain canonicalization** — meaningful infra discipline but zero user-facing surface. good for an internal runbook post.
- **Orphan-collision defense in replenish-pool** — ops-level; a user never sees this directly.
- **Atomic `health_status` on terminal flips** — important correctness fix but needs more context than a tweet allows to be meaningful.
- **Docs-only commits** (lying-DB census refresh, Phase C reset list, reconcile deadline PRD) — no shipped behavior; omitted.
- **Agent Self-Compaction Architecture §14** — V3+ roadmap, not shipped. mentioning roadmap items before they're real violates Cooper's voice.

**Tweets that would benefit from screenshots:**

- Tweet covering the SOUL.md truncation fix (Variant B tweet 4-5) — a before/after showing the truncated vs. full bootstrap context length would make the "silent drop" visceral.
- Tweet covering /edge rebrand (Variant A tweet 6 / Variant B tweet 11) — the new /edge landing page, especially the login-state-aware routing and the open graph card.
- Tweet covering the ack UX (Variant B tweet 3) — a side-by-side of the old 3-minute silence vs. the new three-layer response flow in Telegram.
- Variant B tweet 7 (reconciler hardening) — a dashboard screenshot of the auto-quarantine alert would make the "persistent failure tracking" concrete.

**Factual claims to verify before posting:**

- The "3 minutes of silence" figure and "9:07 PM 2026-05-11" timestamp — confirm this is the real session, not a synthetic test case, before posting publicly.
- "1,000 attendees" for Edge Esmeralda and the "2026-05-30" ship date — confirm this is public information before posting.
- "12 of 44 healthy cv≥88 VMs" for the lying-DB rate — confirm the 44 figure reflects the current fleet accurately (the changelog says "12 of 44" but also references "149 VMs" total from v62→v88; clarify the scoped denominator).
- "~2,000-line PRD" for the ack UX — a rough line count; verify or soften to "~2,000 lines" vs. an exact figure.
- The cv=82 unstick claim in stepSystemdUnit — the commit message says "likely cv=82 unstick." if it's not confirmed in prod, soften to "suspected cv=82 unstick" in Variant B tweet 7.
- gbrain: confirm whether "gbrain" is a name that can be mentioned publicly, or whether it should be anonymized/genericized in the thread.

## Style audit

All banned tokens passed. Manual review still recommended.
