# X-post drafts — v95 (2026-05-13)

Generated by `scripts/generate-version-post.ts` (mode: release, model: claude-sonnet-4-6).
Source changelog: `docs/changelog-latest.md`.
Style guide: `docs/x-post-style-guide.md`.

---

## Variant A — tight 5-7 tweet thread (user-facing wins)

1/

@instaclaws v92 → v95 🦀 — 3 days, 95 commits, no downtime.

⚡ your agent now acknowledges you in Telegram — no more 3-minute silences
🩹 edge_city agents were running on a silently truncated SOUL.md — fixed
📉 lying-DB rate: 27.3% → 0.8% in 48 hours
🛡️ Privacy Mode hardened — chattr +i lockdown, admin kill switch

the release where the fleet stopped lying to itself.

2/

⚡ three-layer acknowledgment UX

before v95: you sent a message, your agent thought for 3 minutes, nothing. then a response appeared. that's a churn experience at scale — Edge Esmeralda has 1,000 attendees shipping 2026-05-30. v95 ships a three-layer Telegram ack so you know your agent heard you. silence is gone.

3/

🩹 silent truncation — fixed

edge_city VMs were provisioned with 36,054 chars of SOUL.md against a 35,000-char ceiling. the last ~1,054 chars — the entire onboarding interview tail and Consensus section — were silently dropped from bootstrap context. all 5 production edge_city VMs affected. v92 fixed it. v93 added the budget-aware guard so it can't happen again.

<!-- exceeds 280 — split or Premium -->

4/

📉 lying-DB rate: 27.3% → 0.8%

12 of 44 healthy VMs had a config-version counter that didn't match reality. reconciler thought they were current; they weren't. root-cause fixes shipped 2026-05-11. fleet sweep + cv-resets ran the same day. census 48 hours later: 1 of 127 VMs. the boring number is the good number.

<!-- exceeds 280 — split or Premium -->

5/

🛡️ Privacy Mode — airtight v0

tightened the SSH bridge allowlist, added an admin kill switch, and locked sensitive paths with `chattr +i`. one-shot deploy closes the vm-354 lockout class. 24h auto-revert still in place. even our team can't read your sessions while it's on.

6/

Next: on-demand provisioning.

schema is live. `buildCloudInitUserdata` is written. the goal: a new VM boots fully configured — no post-provision configure step, no partial-failure recovery loop, no Rule 33 needed.

95 commits. 3 days. Onward.

-----

## Variant B — detailed 14-tweet thread (technical audience)

1/

@instaclaws v92 → v95 🦀 — 95 commits, 3 manifest versions, 3 days.

⚡ three-layer Telegram acknowledgment — no more silent agents
🩹 SOUL.md silent truncation fixed on all edge_city VMs
📉 lying-DB rate 27.3% → 0.8% (48-hour turnaround)
🛡️ Privacy Mode hardened — chattr +i + admin kill switch
🌐 /edge-city → /edge rebrand live for Edge Esmeralda
📜 9 new CLAUDE.md rules (Rules 36–44), each tracing to a real incident

the release where the fleet stopped lying to itself.

2/

3 days. 3 manifest versions. 95 commits — 30 reconciler, 48 infrastructure, 4 user-facing feature, 7 Edge City partner, 6 docs/PRD. 9 new CLAUDE.md rules codified. 1 on-demand provisioning schema shipped. 12 ghost-row paths closed. 46-check bake validation expanded. lying-DB rate: 27.3% → 0.8%.

all live to a fleet of paying-customer VMs the whole time.

3/

⚡ v95 — three-layer acknowledgment UX

originating incident: 3 minutes of silence between a prompt and the agent's 9:07 PM response on 2026-05-11. edge_city VMs are preview infrastructure for Edge Esmeralda — 1,000 attendees, ships 2026-05-30. that silence would have churned that experience at scale. v95 ships a three-layer Telegram ack pipeline. PRD: docs/prd/agent-acknowledgment-ux-2026-05-11.md (~2,000 lines).

<!-- exceeds 280 — split or Premium -->

4/

🩹 SOUL.md silent truncation — v92 + v93

pre-v92 edge_city VMs had 36,054 chars of SOUL.md against a `BOOTSTRAP_MAX_CHARS` ceiling of 35,000. the last ~1,054 chars — onboarding interview tail + entire Consensus section — were silently cut from bootstrap context. all 5 production edge_city VMs affected.

v92 migrated the content. emergency bandaid: `bootstrapMaxChars` 35,000 → 40,000. v93 added the APPEND branch — if a partner section is missing (provisioned before the template existed, or configure failure), it now adds it instead of treating absence as a no-op.

<!-- exceeds 280 — split or Premium -->

5/

📉 lying-DB: 27.3% → 0.8%

census 2026-05-11: 12 of 44 healthy cv≥88 VMs had a config-version counter the DB believed was current but wasn't — reconciler skipped them silently. root-cause fixes shipped same day. Phase 4 cv-resets ran on the 10 worst offenders. census 2026-05-13: 1 of 127 VMs. 48 hours, 33.1× improvement.

<!-- exceeds 280 — split or Premium -->

6/

🛡️ Privacy Mode — airtight v0

previous bridge had gaps. v0 ships: tightened SSH command allowlist + sensitive-path denylist, admin kill switch, `chattr +i` lockdown on protected paths, cutover abort guard (skips bypass keys, halts if none found). one-shot bridge deploy closes the vm-354 lockout class entirely. 24h auto-revert unchanged. toggle at /dashboard/privacy.

<!-- exceeds 280 — split or Premium -->

7/

⚙️ reconciler discipline — five fixes that matter

`stepSystemdUnit` now uses md5 hash compare — likely root cause of the cv=82 unstick cohort. validate-before-restart guards against schema-rejection crashes (includes `NVM_PREAMBLE` so the validation command actually runs). `stepExecStartAlignment` permanently guards against stale Node-path `ExecStart`. inline dispatch scripts bypass Next 15 NFT `.sh` bundling — configure was silently failing on a slice of the fleet.

<!-- exceeds 280 — split or Premium -->

8/

🩹 ghost rows — closed

`assigned_to` wasn't cleared on VM termination — root cause of ghost rows that let a released VM appear assigned. fixed. then audited every adjacent path: 12 more ghost-row entry points found and closed. `health_status` flips on terminal states are now atomic. `ip_address` nulled at `status='failed'` to kill IP-reuse resurrection. FK atomicity race on terminal-flip fixed by splitting the stamp.

<!-- exceeds 280 — split or Premium -->

9/

🩹 gateway proxy — silent model downgrade stopped

the gateway proxy was silently routing real user messages to MiniMax on a slice of the fleet. no error, no log visible to the user — just degraded responses. fixed. the right fix was "stop hiding the routing decision."

10/

🩺 onboarding loop — Rule 33 + Rule 35

post-checkout configure partial-failures were leaving VMs in a stuck loop. Rule 33 now breaks that loop and recovers. Rule 35 extends the gate: channel-credential validator runs at configure time — a VM with a missing or mismatched `telegram_bot_token` on disk gets caught before it ever reaches a user. one-shot Telegram token fix also deployed for VMs already missing `channels.telegram.botToken`.

<!-- exceeds 280 — split or Premium -->

11/

🌐 /edge live for Edge Esmeralda

/edge-city → /edge rebrand shipped. Edge City visual language across /edge, /signup, /connect. page responds to login state — returning users see a different flow than new visitors. branded Open Graph share card live. instaclaw.io/edge.

12/

📈 bake toolchain hardened

canonical fresh-nanode bake toolchain now includes cleanup, validation, and a runbook. validation expanded to 46 checks. brittle version-pin regexes replaced with `semverGte`. `SNAPSHOT_BAKED` cross-reference added. the bake is now auditable, not just runnable.

13/

📜 discipline codified — Rules 36–44

9 new CLAUDE.md rules, each tracing to a real incident. Rule 34: self-heal disk↔DB `telegram_bot_token` mismatch on every reconcile. Rule 44: cv=91 cohort stuck because of strict-180s reconcile deadline — Vercel cron structurally cannot catch up multi-version drift; PRD written. the fleet is more boring to operate every week.

14/

Next: on-demand provisioning.

schema is live (`cloud_init_outcomes`, RLS, circuit breakers). `buildCloudInitUserdata` bootstrap written. goal: a VM boots fully configured from cloud-init — no post-provision configure step, no partial-failure recovery, no Rule 33 edge case to hit.

95 commits. 3 days. Onward.

-----

## Variant C — single banger tweet

@instaclaws v92 → v95 🦀 — 3 days, 95 commits. SOUL.md was silently truncating the last 1,054 chars for all 5 edge_city VMs. lying-DB rate was 27.3% — fixed to 0.8% in 48h. agents now acknowledge you in Telegram instead of going silent for 3 minutes. Privacy Mode locked down with chattr +i. Onward.

<!-- exceeds 280 — split or Premium -->

-----

## Notes

**intentionally excluded and why:**

- `WORKSPACE_AGENTS_MD_V2` trim (18,812 → 13,919 chars) — internal context-window optimization with no direct user-facing outcome to state plainly; could be added to Variant B as a ⚡ perf item if desired.
- gbrain install pipeline / Phase 4 rollout — the changelog doesn't explain what gbrain does for users in terms reviewers outside the team would understand; included only as a reconciler step reference rather than a standalone topic. recommend adding a plain-language description of gbrain before posting anything about it publicly.
- `matchpool-outcomes-ingest` §5.2 — 49-file merge but the user-visible outcome (better matching engine data pipeline) isn't yet shipped to users; omitted to avoid implying a live feature.
- `process.exit()` bypasses `finally` / cron lock leaks — real incidents but too internally technical for even Variant B without more context on what a leaked cron lock costs users.
- SOLA integration removal — dead integration cleanup; not worth tweet real-estate.
- pixel-art Healdsburg village spec (§4.14) — docs/design artifact, not a shipped thing.
- automated changelog + X-post generator (meta) — Cooper can decide whether to mention this; omitted for now as it reads as navel-gazing.
- `--no-strict` flag for fleet rollout — ops tooling; no user impact.

**tweets that would benefit from screenshots:**

- tweet covering the three-layer acknowledgment UX (Variant A/2, Variant B/3) — a short screen recording of Telegram showing the ack appearing would make this concrete.
- tweet covering /edge rebrand (Variant B/11) — before/after screenshot of instaclaw.io/edge with Edge City visual language.
- lying-DB rate improvement (Variant A/4, Variant B/5) — a simple chart: 27.3% on 05-11, 0.8% on 05-13. two data points, but the slope tells the story.

**factual claims to verify before posting:**

- "all 5 production edge_city VMs affected" — confirm this count is still accurate and that none were added/removed between v92 and posting.
- "1,000 attendees" for Edge Esmeralda — confirm this is public information and the correct figure.
- "1 of 127 VMs" for the 0.8% lying-DB figure — the changelog says "0.8% rate" and "12 of 44" for the before state; verify the denominator (127) is correct for the after-census fleet size, or replace with the raw percentage if the absolute count isn't confirmed public.
- "~2,000 lines" for the acknowledgment UX PRD — pulled from the commit message; worth a quick line count before posting.
- Edge Esmeralda ships 2026-05-30 — confirm this date is intended to be public.

## Style audit

All banned tokens passed. Manual review still recommended.
